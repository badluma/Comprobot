name: Publish

on:
  workflow_run:
    workflows: ["Test macOS", "Test Windows", "Test Linux"]
    types: [completed]
    branches: [main]
  workflow_dispatch:

jobs:
  gate:
    name: "Check all tests passed"
    runs-on: ubuntu-latest
    if: github.event_name == 'workflow_dispatch' || github.event.workflow_run.conclusion == 'success'
    outputs:
      proceed: ${{ steps.check.outputs.proceed }}
    steps:
      - name: Verify all test workflows passed on this commit
        id: check
        uses: actions/github-script@v7
        with:
          script: |
            if (context.eventName === 'workflow_dispatch') {
              core.setOutput('proceed', 'true');
              return;
            }

            const sha = context.payload.workflow_run.head_sha;

            const { data: commit } = await github.rest.repos.getCommit({
              owner: context.repo.owner,
              repo: context.repo.repo,
              ref: sha,
            });
            if (!commit.files?.some(f => f.filename === 'pyproject.toml')) {
              core.setOutput('proceed', 'false');
              return;
            }

            const getVersion = async (ref) => {
              const { data: file } = await github.rest.repos.getContent({
                owner: context.repo.owner,
                repo: context.repo.repo,
                path: 'pyproject.toml',
                ref,
              });
              const content = Buffer.from(file.content, 'base64').toString();
              return content.match(/^version\s*=\s*"(.+)"/m)?.[1];
            };

            const currentVersion = await getVersion(sha);
            const parentSha = commit.parents[0]?.sha;
            const parentVersion = parentSha ? await getVersion(parentSha) : null;

            if (!currentVersion || currentVersion === parentVersion) {
              core.setOutput('proceed', 'false');
              return;
            }

            const names = ['Test macOS', 'Test Windows', 'Test Linux'];
            const { data: { workflows } } = await github.rest.actions.listRepoWorkflows({
              owner: context.repo.owner,
              repo: context.repo.repo,
            });

            for (const name of names) {
              const wf = workflows.find(w => w.name === name);
              if (!wf) {
                core.setOutput('proceed', 'false');
                return;
              }
              const { data: { workflow_runs } } = await github.rest.actions.listWorkflowRuns({
                owner: context.repo.owner,
                repo: context.repo.repo,
                workflow_id: wf.id,
                head_sha: sha,
                status: 'completed',
                per_page: 1,
              });
              if (!workflow_runs.length || workflow_runs[0].conclusion !== 'success') {
                core.setOutput('proceed', 'false');
                return;
              }
            }

            core.setOutput('proceed', 'true');

  release:
    name: "Create GitHub Release"
    needs: gate
    if: needs.gate.outputs.proceed == 'true'
    runs-on: ubuntu-latest
    permissions:
      contents: write
    outputs:
      version: ${{ steps.version.outputs.version }}
    steps:
      - uses: actions/checkout@v4
        with:
          ref: ${{ github.event.workflow_run.head_sha || github.sha }}

      - name: Extract version
        id: version
        run: |
          VERSION=$(grep '^version' pyproject.toml | sed 's/version = "\(.*\)"/\1/')
          echo "version=$VERSION" >> $GITHUB_OUTPUT

      - name: Create release
        uses: softprops/action-gh-release@v2
        with:
          tag_name: v${{ steps.version.outputs.version }}
          make_latest: true
          generate_release_notes: true

  pypi:
    name: "Publish to PyPI"
    needs: release
    runs-on: ubuntu-latest
    environment: pypi
    permissions:
      id-token: write
    steps:
      - uses: actions/checkout@v4
        with:
          ref: ${{ github.event.workflow_run.head_sha || github.sha }}

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.10"

      - name: Install build
        run: pip install build

      - name: Build package
        run: python -m build

      - name: Publish to PyPI
        uses: pypa/gh-action-pypi-publish@release/v1

  brew:
    name: "Update Homebrew Formula"
    needs: release
    runs-on: ubuntu-latest
    steps:
      - name: Update formula
        env:
          VERSION: ${{ needs.release.outputs.version }}
          TAP_TOKEN: ${{ secrets.TAP_TOKEN }}
        run: |
          URL="https://github.com/badluma/Comprobot/archive/refs/tags/v${VERSION}.tar.gz"
          SHA256=$(curl -sL "$URL" | sha256sum | cut -d' ' -f1)

          git clone "https://x-access-token:${TAP_TOKEN}@github.com/badluma/homebrew-homebrew.git"
          cd homebrew-homebrew

          sed -i "s|url \".*\"|url \"${URL}\"|" Formula/comprobot.rb
          sed -i "s|sha256 \".*\"|sha256 \"${SHA256}\"|" Formula/comprobot.rb

          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add Formula/comprobot.rb
          git commit -m "chore: update comprobot to v${VERSION}"
          git push
