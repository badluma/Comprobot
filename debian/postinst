#!/bin/bash
set -e

case "$1" in
    configure)
        pip3 install --quiet --break-system-packages --upgrade \
            "discord.py[voice]" \
            python-dotenv \
            "tomlkit>=0.12.0" \
            ollama \
            google-genai \
            groq \
            InquirerPy
        ;;
esac

exit 0
